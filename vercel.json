{
    "builds": [
        {
            "src": "./app/app.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/app/app.ts"
        }
    ]
}