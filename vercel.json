{
    "version": 2,
    "builds": [
        {
            "src": "app/app.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "app/app.ts"
        }
    ]
}